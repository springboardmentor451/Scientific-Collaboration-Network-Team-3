name: SCNA CI

on:
  push:
    branches:
      - main
      - Manshi-kumari
      - Guru-Prasath
      - Sneha-Mahato
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build SCNA
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build